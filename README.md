# MyMap-Data-Structure
Class MyMap uses an ArrayList of LinkedLists(that have a Pair) to mimic a HashTable.   Using the HashTable, class MyMap has inner class Pair that acts as the &lt;key, value> pair.
